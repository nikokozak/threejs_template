/**
 * A global configuration object.
 *
 * Place configuration you want to access globally here.
 *
 * Useful for parametrizing with dat.GUI or similar.
 */

export const params = {
    width: window.innerWidth,
    height: window.innerHeight,
    viewAngle: 75,
}
